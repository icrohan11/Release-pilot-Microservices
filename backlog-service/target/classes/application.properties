# Identify this service and pull the rest of the configuration from the Config Server.
spring.application.name=backlog-service
spring.config.import=optional:configserver:http://localhost:8888

# Local fallbacks (used only if the Config Server is unreachable)
server.port=9003
